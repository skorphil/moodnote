import type { ScaleTranslations } from "../../../types";

const scale: ScaleTranslations = {
  "scale.0": "Not At All",
  "scale.1": "A Little",
  "scale.2": "Moderate",
  "scale.3": "Quite A Lot",
  "scale.4": "Extremely",
};

export default scale;
